# /etc/nginx/sites-available/mirror
# Serves the local mirror root over plain HTTP on the LAN.
server {
    listen 80 default_server;
    server_name _;
    root /srv/mirror;
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;
}
